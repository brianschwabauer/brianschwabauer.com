User-agent: *
Allow: /
Disallow: /admin
Disallow: /signin
Disallow: /archive
Disallow: /api/

# AI crawlers are welcome to the public pages. Named explicitly rather than
# left to the wildcard above so the permission is a decision on the record,
# not an accident of omission — several of these look for their own
# user-agent first and only fall back to `*` if there is no group for them.
#
# A named group REPLACES the wildcard group for that crawler rather than
# adding to it, so the Disallow lines below are repeated deliberately. Drop
# them and these bots would be invited into /admin and /api/.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: PerplexityBot
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: CCBot
Allow: /
Disallow: /admin
Disallow: /signin
Disallow: /archive
Disallow: /api/

Sitemap: https://brianschwabauer.com/sitemap.xml
